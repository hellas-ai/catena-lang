(theory type nat {
  # products
  (arr * : 2 -> 1)
  (arr 1 : 0 -> 1)
})

(theory program type {
  (arr unit.intro : [] -> 1)
  (arr unit.elim : 1 -> [])
  (arr unit.left-elim : {[a.]
    ({1 [.a]} *)
  } -> ([a . a]))
  (arr *.intro : [a b] -> *)
  (arr *.elim : * -> [a b])
})
