# Shapes describe coordinate bounds and preserve their logical rank.
(theory type nat {
  (arr shape.1d : 1 -> 1)
  (arr shape.2d : 2 -> 1)
  (arr shape.3d : 3 -> 1)
  (arr ix : 1 -> 1)
})

(theory program type {
  (arr ix.copies2 : {[index-shape.]
    ([.index-shape] ix val)
  } -> {[index-shape.]
    ([.index-shape] ix val)
    ([.index-shape] ix val)
  })

  (arr ix.forget : {[index-shape.]
    ([.index-shape] ix val)
  } -> ([index-shape.] []))

  (arr ix.2d.intro : {[first-size second-size.]
    ({({[.first-size]} shape.1d)} ix val)
    ({({[.second-size]} shape.1d)} ix val)
  } -> {[first-size second-size.]
    ({({[.first-size second-size]} shape.2d)} ix val)
  })

  (arr ix.2d.elim : {[first-size second-size.]
    ({({[.first-size second-size]} shape.2d)} ix val)
  } -> {[first-size second-size.]
    ({({[.first-size]} shape.1d)} ix val)
    ({({[.second-size]} shape.1d)} ix val)
  })

  # Return the row-major linear offset of an index, independently of rank.
  (arr ix.to-u64 : {[index-shape.]
    ([.index-shape] ix val)
  } -> {[index-shape.]
    ([.index-shape] ix val)
    (u64 val)
  })

  # Refine a scalar into a bounded one-dimensional index.
  (arr u64.to-ix : {[index bound.]
    ({[.index] u64} :)
    ({[.bound] u64} :)
    ([.index bound] u64.lt |-)
  } -> {[index bound.]
    ({[.index] ({({[.bound]} shape.1d)} ix)} :)
  })
})
