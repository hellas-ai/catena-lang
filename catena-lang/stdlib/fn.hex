# Function pointers
(theory type nat {
  # (A ~> B) is a function *pointer* - has no captured vars
  (arr -> : 2 -> 1)

  # traditional "eval" for function types; *fully* applies a function
  # A ● (A ~> B) -> B
  (arr eval : 2 -> 1)
})

# Eliminators of function pointers
(theory program type {
  # Reduce over indices with a zero value, an accumulator closure, and an
  # index-to-value closure.
  #
  #   reduce : A ● (A * A => A) ● (ix n => A) ● (n : u64) -> A
  (arr reduce : {[a n.]
    ([.a])
    ([.a a] {* [.a]} =>)
    ({([.n] ix val) ([.a])} =>)
    ({[.n] u64} :)
  } -> ([a n . a]))

  # Closure-converted reduce.
  #
  #   reducec : A ● X ● (X * A * A -> A) ● Y ● (Y * ix n -> A) ● (n : u64) -> A
  (arr reducec : {[x y a n.]
    ([.a])
    ([.x])
    {([.x a] * [xa.]) ([.xa a] * [xaa.]) ([.xaa a] -> val)}
    ([.y])
    {({([.y]) ([.n] ix val)} * [yi.]) ([.yi a] -> val)}
    ({[.n] u64} :)
  } -> ([x y a n . a]))
})
