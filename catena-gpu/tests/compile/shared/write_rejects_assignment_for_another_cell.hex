(def program shared-write-with-assignment-for-another-cell : {[
  grid block shared slot size phase schedule thread cell another-cell element.
]
  ({[.thread] ([.grid] gpu.thread)} :)
  ({[.block] ({[.grid block]} gpu.block)} :)
  ({[.shared] ({[.shared block slot size element]} gpu.shared)} :)
  ({[.cell] ({({[.size]} shape.1d)} ix)} :)
  ([.element] val)
  ({[.schedule thread another-cell shared]} gpu.shared.assigned |-)
  ({[.phase thread cell shared]} gpu.shared.owns |-)
} -> {[
  grid block shared slot size phase schedule thread cell another-cell element.
]
  ({[.thread] ([.grid] gpu.thread)} :)
  ({[.block] ({[.grid block]} gpu.block)} :)
  ({[.shared] ({[.shared block slot size element]} gpu.shared)} :)
  ({[.cell] ({({[.size]} shape.1d)} ix)} :)
  ({[.schedule thread cell shared]} gpu.shared.assigned |-)
  ({[.phase thread cell shared]} gpu.shared.owns |-)
} = gpu.shared.write)
