// Closure conversion v2:

// Find regions by following closure sources to targets
mod region;

// Create new definitions in the theory from each closure regions
mod definition;

// Replace closure regions with names, environments, and context operations
mod replace;
